/** End marker radius: an 8px dot, the smallest a reader reliably spots. */
export const END_DOT_RADIUS = 4;

/** Hovered-point radius: one step bigger than the end marker so hover reads as a response. */
export const ACTIVE_DOT_RADIUS = 5;

/** Width of the card-coloured ring that keeps a dot legible where it crosses the line. */
export const RING_WIDTH = 2;

/** Pixel gap Recharts keeps between x labels before it starts dropping some. */
export const X_TICK_MIN_GAP = 28;

/** Room the y-axis labels take on the left ("100" at 11px). */
export const Y_AXIS_WIDTH = 30;
