import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export function Rules() {
  return <ScreenPlaceholder title="Rules & standards" subtitle="The rule library and composer land in Phase 3." />;
}
