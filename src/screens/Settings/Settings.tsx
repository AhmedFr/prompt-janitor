import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export function Settings() {
  return <ScreenPlaceholder title="Settings" subtitle="Folders, schedule, alerts, and more land in Phase 3." />;
}
